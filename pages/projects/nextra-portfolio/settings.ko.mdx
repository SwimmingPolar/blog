import { Tree, Folder, File, Screenshot } from "@/components";
import "@code-hike/mdx/dist/index.css";

# 현재 설정

## 1. 기본 레이아웃 수정

- home page
  1. `navbar` 위에 배너 이미지를 노출시킴
  2. 배너 이미지 노출로 `mob ile`에서 `sidebar`레이아웃이 틀어지는 걸 방지하기 위해
     `sidebar`의 `transition`을 없애고, `z-index`등등 여러가지 복합적인`styles`를 수정함
- other page
  1. `footer`의 위치를 수정해서 `navbar` 밑으로 가게 함
  2. `footer`의 하단 `div`를 보이지 않게 수정함

## 2. i18n

## 3. CodeHike

<CH.Code>

```js app.js
function lorem(ipsum, dolor = 1) {
  const sit = ipsum == null ? 0 : ipsum.sit;
  dolor = sit - amet(dolor);
  return sit ? consectetur(ipsum) : [];
}
```

</CH.Code>

## 4. `nextra docs` 컴포넌트 클론

`nextra docs`에서 사용하는 기본적인 컴포넌트들을 훔쳐옴

<Tree>
  <Folder name="Components" defaultOpen>
    <File name="FileTree" />
    <File name="Card" />
    <File name="Table" />
    <File name="Video" />
    <File name="Screenshot" />
  </Folder>
</Tree>

등등 사용하기 편하고 예쁘게 만들어져있길래 가져옴

## 5. tailwind, postcss, autoprefixer 추가

4번을 지원하기 위해 추가함

## 5. storybook 추가
